# Root config is kept for reference but not used when the site source is set to docs/
# Use `bundle exec jekyll serve --source docs --config docs/_config.yml --watch`

title: Stop Transcoding Docs
theme: minima
markdown: kramdown
plugins: []

# If you use GitHub Pages docs folder source, set this repo's Pages source to /docs
baseurl: ""
url: "https://stop-transcoding.mstrhakr.com"

destination: "_site"
